
The /top directory is not a real directory.

The HttpModule Wellcome.Library.WebForms.Meta.UvVersionModule rewrites any paths under here to use the
latest build of the UV in the sibling folder.

Do not delete the folder - but also, don't deploy a UV build into it.

Content in this directory inherits the cache control directives from /spas/uv/